/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'placehold.co', 'randomuser.me' , 'res.cloudinary.com'],
  },
};

module.exports = nextConfig;
